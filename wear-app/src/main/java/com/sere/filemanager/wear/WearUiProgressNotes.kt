package com.sere.filemanager.wear

object WearUiProgressNotes {
    const val remoteDashboard = "Remote dashboard shows URL, PIN, network and battery directly on the watch."
    const val remoteSettings = "Remote settings can be edited on watch and synced from phone."
    const val permissions = "Permission hub avoids duplicated grant buttons after access is already granted."
}
