package com.sere.filemanager.core.remote

object RemoteWebMessages {
    const val uploadsDisabled = "Uploads are disabled in server settings."
    const val destructiveDisabled = "Delete and rename are disabled in server settings."
    const val invalidPin = "Invalid PIN."
    const val invalidPath = "Invalid or unsafe path."
    const val networkRequired = "Connect to a trusted local network before starting remote access."
}
