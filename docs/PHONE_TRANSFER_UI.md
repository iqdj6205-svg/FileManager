# Phone Transfer UI

## Added

- `PhoneTransferState`
- `PickedFile`
- `PhoneTransferController`
- file metadata lookup from content URI
- ChannelClient send wrapper for selected files

## Next

- Add Activity Result file picker into `phone-app` UI.
- Add transfer progress screen.
- Send metadata command before ChannelClient stream so the watch can preserve original filenames and target paths.
